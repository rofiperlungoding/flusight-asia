name: FluSight Data Pipeline

on:
  schedule:
    - cron: '0 0 * * 0'
  workflow_dispatch:
    inputs:
      max_sequences:
        description: 'Maximum sequences to fetch'
        required: false
        default: '100'
      dry_run:
        description: 'Dry run (skip database writes)'
        required: false
        default: 'false'

env:
  PYTHON_VERSION: '3.11'
  SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
  SUPABASE_SERVICE_KEY: ${{ secrets.SUPABASE_SERVICE_KEY }}
  NCBI_EMAIL: ${{ secrets.NCBI_EMAIL }}
  NCBI_API_KEY: ${{ secrets.NCBI_API_KEY }}

jobs:
  ingest-sequences:
    name: 🧬 Ingest Sequences
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: pipeline

    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v4

      - name: 🐍 Set up Python ${{ env.PYTHON_VERSION }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: 'pip'
          cache-dependency-path: pipeline/pyproject.toml

      - name: 📦 Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install biopython requests python-dotenv pandas numpy tqdm pydantic httpx

      - name: 🔬 Fetch and parse sequences
        id: fetch
        run: |
          python << 'EOF'
          import sys
          sys.path.insert(0, 'src')
          from flusight.ingestion.ncbi import NCBIFetcher
          import os
          import json

          email = os.environ.get('NCBI_EMAIL')
          api_key = os.environ.get('NCBI_API_KEY')
          max_sequences = int('${{ github.event.inputs.max_sequences }}' or '100')

          print(f'📧 NCBI Email: {email[:5]}***')
          print(f'🔢 Max sequences: {max_sequences}')

          fetcher = NCBIFetcher(email=email, api_key=api_key)
          query = fetcher.build_search_query(subtype='H3N2', segment='HA', region='Asia')
          
          print(f'🔍 Query: {query[:80]}...')
          
          ids = fetcher.search(query, max_results=max_sequences)
          print(f'✅ Found {len(ids)} sequence IDs')

          if not ids:
              print('⚠️ No sequences found')
              with open('sequences_data.json', 'w') as f:
                  json.dump([], f)
              sys.exit(0)

          print(f'📥 Fetching sequences from NCBI...')
          sequences = []
          
          for i, record in enumerate(fetcher.fetch_records(ids)):
              parsed = fetcher.parse_record(record)
              sequences.append(parsed)
              if (i + 1) % 25 == 0:
                  print(f'  Processed {i + 1}/{len(ids)}...')

          print(f'✅ Parsed {len(sequences)} sequences')

          with open('sequences_data.json', 'w') as f:
              json.dump(sequences, f, default=str, indent=2)

          print('💾 Saved sequences to sequences_data.json')
          EOF

      - name: 💾 Write to Supabase
        if: github.event.inputs.dry_run != 'true'
        run: |
          python << 'EOF'
          import os
          import json
          import httpx
          from datetime import datetime

          # Get env vars
          supabase_url = os.environ.get('SUPABASE_URL', '').rstrip('/')
          supabase_key = os.environ.get('SUPABASE_SERVICE_KEY', '')

          if not supabase_url or not supabase_key:
              print('⚠️ Supabase secrets not configured - skipping database write')
              exit(0)

          # Load parsed sequences
          with open('sequences_data.json', 'r') as f:
              sequences = json.load(f)

          if not sequences:
              print('⚠️ No sequences to write')
              exit(0)

          print(f'📝 Writing {len(sequences)} sequences to Supabase...')

          # Setup headers
          headers = {
              'apikey': supabase_key,
              'Authorization': f'Bearer {supabase_key}',
              'Content-Type': 'application/json',
              'Prefer': 'resolution=merge-duplicates,return=representation',
          }

          rest_url = f'{supabase_url}/rest/v1'
          written = 0
          errors = 0

          for seq in sequences:
              try:
                  record = {
                      'genbank_id': seq.get('genbank_id'),
                      'strain_name': seq.get('strain_name'),
                      'segment': seq.get('segment', 'HA'),
                      'subtype': seq.get('subtype', 'H3N2'),
                      'raw_sequence': seq.get('raw_sequence'),
                      'sequence_length': seq.get('sequence_length'),
                      'collection_date': seq.get('collection_date'),
                      'host': seq.get('host', 'Human'),
                      'source': seq.get('source', 'ncbi'),
                  }
                  
                  # Remove None values
                  record = {k: v for k, v in record.items() if v is not None}
                  
                  response = httpx.post(
                      f'{rest_url}/sequences',
                      headers=headers,
                      params={'on_conflict': 'genbank_id'},
                      json=[record],
                      timeout=30.0,
                  )
                  
                  if response.status_code in [200, 201]:
                      written += 1
                  else:
                      print(f'⚠️ HTTP {response.status_code}: {response.text[:100]}')
                      errors += 1
                      
              except Exception as e:
                  print(f'❌ Error: {e}')
                  errors += 1

          print(f'✅ Written: {written}')
          print(f'❌ Errors: {errors}')

          # Log to pipeline_logs table
          try:
              log_record = {
                  'job_name': 'ncbi_ingest',
                  'status': 'success' if errors == 0 else 'partial',
                  'records_processed': written,
                  'records_failed': errors,
                  'message': f'Wrote {written} sequences, {errors} errors',
                  'started_at': datetime.utcnow().isoformat(),
                  'completed_at': datetime.utcnow().isoformat(),
              }
              httpx.post(
                  f'{rest_url}/pipeline_logs',
                  headers={**headers, 'Prefer': 'return=representation'},
                  json=[log_record],
                  timeout=10.0,
              )
              print('📋 Logged to pipeline_logs')
          except Exception as e:
              print(f'⚠️ Could not log: {e}')
          EOF

      - name: 📤 Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: sequence-data
          path: pipeline/sequences_data.json
          retention-days: 7
